import Portal from "../../../../components/protal";

const PositionTooltip = ({ show, x, y, offsetX, offsetY, zIndex }) => {
  return <Portal id="portal-root">
    
  </Portal>
};

export default PositionTooltip;